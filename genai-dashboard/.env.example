# Copy to .env.local (frontend) and/or .env (backend) and fill in real values.
# Never commit the filled-in .env / .env.local files.

# Groq API key used by backend/server.py for script + choice generation.
GROQ_API_KEY=

# Hugging Face token used by backend/server.py to call the Wan 2.1 video model.
HF_TOKEN=
