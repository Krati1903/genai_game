# Copy to backend/.env and fill in real values. Never commit the filled-in file.

# Groq API key (script + choice generation): https://console.groq.com/keys
GROQ_API_KEY=

# Hugging Face token (Wan 2.1 video generation): https://huggingface.co/settings/tokens
HF_TOKEN=
